# CLAUDE.md — חוקת הפרויקט (Base / Constitution)

> קובץ זה נטען אוטומטית לכל סשן. **כל הסוכנים תחת `.claude/agents/` יורשים ממנו.**
> כללים כאן גוברים על התנהגות ברירת מחדל. סוכן שסותר את החוקה — פסול.

---

## 1. שפה ותקשורת
- **תשובות למשתמשת: בעברית.**
- **קוד, שמות משתנים, שמות קבצים, commit messages: באנגלית.**
- הודעות log למשתמשת בזמן ריצה: עברית ברורה (איזה פרסר נבחר, כמה שורות זוהו/נדחו ולמה).

## 2. עקרון עבודה — לא לנחש
- **לפני כל שינוי: לקרוא את הקוד הקיים.** לא להניח איך פונקציה עובדת — לפתוח אותה.
- **אחרי כל שינוי: להריץ ולאמת מול נתונים אמיתיים**, לא מול הנחות. במודול הפיננסי — מול קובץ PDF/Excel אמיתי.
- אם לא ניתן לאמת — לומר זאת במפורש, לא להצהיר "עובד".

## 3. סטנדרט קוד
- **TypeScript strict. בלי `any`** (השתמש ב־`unknown` + narrowing או בטיפוס מפורש).
- **בלי לוגיקה כפולה.** אותו חישוב לא נכתב פעמיים.
- **טיפוסים משותפים במקום אחד**: backend ב־`backend/src/types/`, frontend ב־`frontend/src/types/models.ts`.
- לכתוב קוד שנקרא כמו הקוד סביבו — אותה צפיפות הערות, אותם שמות, אותו idiom.

## 4. מקור אמת יחיד (Single Source of Truth)
- **אין חישוב כפול של אותו נתון בשני מקומות.** כל חישוב פיננסי יושב בשכבת שירות אחת; ה־UI רק צורך אותה.
- מקורות האמת הקיימים (לא לשכפל, לצרוך):
  - **סכומי כסף חודשיים** → `backend/src/modules/dashboard/dashboard.repository.ts` (מסכם `expenses` + confirmed non-financing credit לפי `billingDate`). `expenses.service.monthSpend` וכל השאר משתמשים בו.
  - **סיווג/קטגוריזציה** → `buildCategorizer` שמיוצא מ־`credit.service`.
  - **מקור אמת לתנועות אשראי מול הוצאות**: ה־`הוצאות` הוא READ-TIME MERGE של `expenses` + credit (ראה `expenses.repository.findCreditByMonth`). **אין להעתיק נתונים בין הטבלאות** — זה יגרום ל־double-count בדשבורד.

## 5. חוקי דומיין פיננסי מחייבים (banker הוא הסמכות)
- **סיווג הכנסה/הוצאה נקבע לפי העמודה הפיזית** (זכות = הכנסה / חובה = הוצאה) — **לא לפי טקסט התיאור**. תיאור משמש רק לקטגוריזציה משנית.
- **אשראי מתגלגל / financing** (`transactionType: "financing"`) הוא מימון פנימי — **מוחרג מכל סכומי ההוצאה**.
- **החזרי הלוואה**: כשהמידע קיים בשורה — לפצל ל**קרן** (principal) ו**ריבית** (interest). ריבית = הוצאה מימונית, לא הוצאה שוטפת.
- ייחוס חודשי של אשראי מאושר תמיד לפי `billingDate`, לא `transactionDate`.
- סכומים מגיעים מ־Prisma Decimal כ־**string** ב־frontend — לטפל בהתאם.

## 6. Definition of Done
משימה סגורה רק כאשר **כל** הבאים מתקיימים:
1. הקוד עובר `npm run build` (frontend) ו־type-check (backend) ללא שגיאות.
2. יש בדיקה/אימות שמכסה את המקרה שטופל.
3. התוצאה אומתה מול קובץ/נתון אמיתי.
4. אין רגרסיה בטאבים/מסכים אחרים.
5. אישור `qa` — ובמשימה פיננסית גם אישור `banker`.

## 7. מה אסור
- לא לגעת בקבצים מחוץ לתחום האחריות של הסוכן **בלי לסמן זאת במפורש** ל־BOSS.
- לא למחוק/לשכתב קוד קיים בלי גיבוי (git) והסבר.
- לא לשנות סכמת DB בלי מיגרציה (`backend/prisma/migrations/`).
- לא להוסיף אינטגרציית Open Banking / חיבור ישיר לחשבון בנק — **החלטת מוצר: לעולם לא.** רכישת נתונים נשארת ידנית (העלאת קבצים).

## 8. סביבת ריצה — gotchas (WSL על /mnt/c)
- **Vite לא עושה hot-reload ב־WSL.** אחרי כל שינוי frontend: `pkill -9 -f "node.*vite"` ואז restart, אחרת המשתמשת רואה קוד ישן.
- **ts-node-dev משאיר תהליכים ישנים על port 3000** (routes 404 מסתורי). לפני restart backend: `pkill -9 -f ts-node-dev; pkill -9 -f src/server.ts; fuser -k 3000/tcp`.
- **לפני שמדווחים על באג בפרסר — לוודא שהשרת טען את הקוד העדכני** (היה מקרה שרץ קוד ישן ו־PDF נשלח בטעות למסלול Excel).
- Stack: React 18 + Vite (5173) · Express + Prisma 7 (3000) · MariaDB (3307, `bash backend/start-db.sh`). Gate login → bearer token; סיסמה `APP_GATE_PASSWORD` ב־`backend/.env`.

## 9. פרוטוקול העברה בין סוכנים (Handoff)
כל סוכן מסיים כל משימה עם הבלוק הזה:
```
### Handoff
- מה שונה:
- קבצים שנגעתי בהם:
- מה נבדק (ואיך אומת):
- מה נשאר פתוח / סיכונים:
- מעביר ל:
```

## 10. שרשרת הפיקוד
`BOSS` הוא הסמכות היחידה שסוגרת משימה. סדר ברירת מחדל:
**BOSS** (מפרק ומקצה) → **banker** (כללים פיננסיים, במשימות נתונים) → **architect** → סוכן ביצוע (**backend**/**frontend**/**fullstack**) → **designer** + **ux** (אם יש UI) → **qa** → **BOSS** (אישור סופי).

הגדרות הסוכנים המלאות: `.claude/agents/*.md`.
