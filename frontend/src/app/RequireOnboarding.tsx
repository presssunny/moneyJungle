import type { ReactNode } from 'react';
/** Setup is progressive. Home explains missing information; access never implies completeness. */
export function RequireOnboarding({children}:{children:ReactNode}) { return <>{children}</>; }
